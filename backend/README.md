# Kudla Backend

Minimal Node.js API using Express.js.

## Run locally

```bash
npm install
npm run dev
```

The API runs at `http://localhost:3000` by default.

## Endpoints

- `GET /` — API welcome response
- `GET /health` — health check

## Tests

```bash
npm test
```
